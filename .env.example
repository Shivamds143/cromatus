# MongoDB connection string, including credentials and the cluster hostname.
MONGODB_URI=mongodb+srv://username:password@cluster.example.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=chromatus2

# Credentials required to open /admin. Use strong, unique values in production.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-long-unique-password
